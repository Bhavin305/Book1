const data = {
    'ABC' : {
        lan: 'en',
        gen: 'SP1'

    },
    'DEF' :{
        lan: 'hindi',
        gen: 'SP2'
    }
}

        {/* <div>
           {
               Object.keys(data[1]).map(movie => { 
                    if (props.filters.language.length) {
                        if(props.filters.language.includes(data[1][movie].EventLanguage)){                       
                            return <Movie movie={data[1][movie]} />
                        } else {
                            return null;
                        }
                    } else {
                        return <Movie movie={data[1][movie]} />
                    }
                    
              })
           }
        </div> */
    }